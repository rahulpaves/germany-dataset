# The paid country files are the product. They must never be uploaded as static
# assets, or anyone can fetch /data-source/italy.json and the gate is decoration.
# The serverless function does not read these: it uses lib/paid-data.js, which is
# generated from them by build_gate.py and bundled into the function.
data-source/
